import styled from '@emotion/styled';

export const HeaderContainer = styled.div`
  display: flex;
  justify-content: space-between;
  padding: 15px 64px;
  background-color: ${({ theme }) => theme.color.featuresColor};
`;
